# =============================================================================
# Linear Issue Duplication Agent Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Linear OAuth Application (REQUIRED)
# -----------------------------------------------------------------------------
# Create an OAuth application at: Linear Settings > API > Applications
# Enable webhooks and select "Issues" under Data change events
# Set the webhook URL to: https://your-domain.com/issue-duplication
LINEAR_CLIENT_ID=your_client_id
LINEAR_CLIENT_SECRET=your_client_secret
LINEAR_WEBHOOK_SECRET=your_webhook_signing_secret

# -----------------------------------------------------------------------------
# Linear API Token (REQUIRED)
# -----------------------------------------------------------------------------
# Use either a Personal API Key or an OAuth access token.
# 
# For Personal API Key (simplest for testing):
#   1. Go to Linear Settings > API > Personal API Keys
#   2. Create a new key and paste it here
#
# For OAuth (recommended for production):
#   Use the access token obtained from the OAuth flow with actor=app
LINEAR_ACCESS_TOKEN=lin_api_xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Duplication Rules (REQUIRED)
# -----------------------------------------------------------------------------
# JSON array defining when and how to duplicate issues.
#
# Each rule has:
#   - name: Human-readable name for logging
#   - triggerLabelName: Label that triggers duplication (case-insensitive)
#   - sourceTeamId: Only issues FROM this team trigger this rule
#   - targetTeams: Array of {name, teamId} for destination teams
#
# Example with two source teams using the same label:
# [
#   {
#     "name": "Team A Mobile Duplication",
#     "triggerLabelName": "Multi-Platform",
#     "sourceTeamId": "team-a-uuid",
#     "targetTeams": [
#       {"name": "iOS", "teamId": "team-a-ios-uuid"},
#       {"name": "Android", "teamId": "team-a-android-uuid"}
#     ]
#   },
#   {
#     "name": "Team B Mobile Duplication",
#     "triggerLabelName": "Multi-Platform",
#     "sourceTeamId": "team-b-uuid",
#     "targetTeams": [
#       {"name": "iOS", "teamId": "team-b-ios-uuid"},
#       {"name": "Android", "teamId": "team-b-android-uuid"}
#     ]
#   }
# ]

DUPLICATION_RULES=[{"name":"Mobile Platform Duplication","triggerLabelName":"Multi-Platform","sourceTeamId":"your-source-team-uuid","targetTeams":[{"name":"iOS","teamId":"your-ios-team-uuid"},{"name":"Android","teamId":"your-android-team-uuid"}]}]

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000
